services:
  - type: web
    name: blind-tasting-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    releaseCommand: alembic upgrade head
